# 🎮 Word Quest - 프로젝트 진행 현황 (Final Project)

## 📅 2026-06-04-2 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🎯 시스템 표준화 및 사용자 설정 정밀화)
- **사용자 설정 실질적 연동:**
    *   **고주사율(FPS) 지원:** `SettingsManager`에서 유저가 선택한 FPS(60, 120, 180)를 시스템의 `preferredRefreshRate`에 동적으로 반영하는 엔진 구현.
    *   **진동 피드백 최적화:** 오답 시 발생하던 강한 진동을 세련된 '소프트 클릭' 방식(50ms 또는 시스템 표준 EFFECT_CLICK)으로 개선하여 불쾌감 제거.
- **기본 로케일 한국어 강제화:**
    *   **전역 언어 설정 변경:** `values-ko-rKR`에 있던 리소스를 기본 `values/strings.xml`로 전면 이관하여, 어떤 시스템 환경에서도 한국어가 기본 노출되도록 강제 설정.
- **날짜 갱신 엔진 표준화:**
    *   **안드로이드 표준 Broadcast 도입:** 불안정한 Runnable 루프를 폐기하고, 시스템 표준 방송인 `ACTION_TIME_TICK` 수신 방식으로 변경하여 배터리 효율과 갱신 정확도 극대화.
- **리소스 무결성 및 하드코딩 완전 제거:**
    *   `settings_dialog.xml` 등 UI 전반에 남아있던 모든 하드코딩 문자열을 리소스화하고, 빌드 오류를 유발하던 특수 기호(`???`)를 안전한 식별자로 최종 교체 완료.
- **사운드 엔진 노이즈 제거:**
    *   동일 곡 중복 요청 시의 리셋 로직을 정밀 제어하여 BGM 전환 시 발생하는 지지직거리는 팝 노이즈(Pop Noise) 완전 해결.

### 🚀 향후 작업 계획 (Next Steps)
1. **점검:** 최종 배포를 위한 전체 기능 및 성능 정밀 점검.

---

## 📅 2026-06-04-1 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🛡️ 시스템 안정성 강화 및 리소스 완전 최적화)
- **데이터베이스 복구 및 안정화:**
    *   **강제 초기화 정책 도입:** 개발 중 발생한 버전 불일치(2 -> 1)로 인한 크래시를 해결하기 위해 `fallbackToDestructiveMigration` 적용.
    *   **실시간 데이터 스트리밍:** `GameViewModel`에서 Room Flow를 LiveData로 변환하여 사용함으로써, 데이터 로딩 시점과 상관없이 UI가 즉각 반응하도록 개선.
- **게임 루프 무결성 확보:**
    *   **레이아웃 노출 로직 정밀화:** 게임 중 오답 판정 등으로 인한 데이터 변경 시 시작 화면(`layoutStart`)이 반복적으로 나타나던 심각한 UX 오류 해결.
    *   **진입 장벽 제거:** 단어 로딩 지연 시 버튼이 비활성화되던 문제를 해결하고, 로딩 상태를 Toast로 안내하도록 변경.
- **사운드 품질 고도화:**
    *   **재생 엔진 최적화:** 동일 곡 재생 시 불필요한 중단/재시작 로직을 제거하여 초기 구동 시의 노이즈(지지직거림) 및 팝 노이즈 해결.
- **리소스 완전 표준화 (Hardcoding 0%):**
    *   **전역 한글화 및 로컬라이징:** 결과창(Score, Best), 카운트다운(START!), 날짜 포맷 등 프로젝트 내 모든 하드코딩 문자열을 `strings.xml`로 이관하고 한국어 리소스 동기화 완료.
    *   **빌드 무결성 확보:** AAPT2 오류를 유발하던 특수 기호(`???`) 리소스를 안전한 식별자(`unknown_answer`)로 교체하여 빌드 안정성 확보.

### 🚀 향후 작업 계획 (Next Steps)
1. **날짜 변경:** 시스템 날짜 연동 방식 및 표시 형식 최종 조정.
2. **여러 리팩토링:** 코드 구조 개선 및 미사용 리소스 정리를 통한 경량화.
3. **점검:** 최종 배포를 위한 전체 기능 및 성능 정밀 점검.

---

## 📅 2026-06-03-12 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: ⚙️ 동적 난이도 엔진 고도화 및 사운드 안정성 종결)
- **동적 난이도 밸런싱 시스템 구축:**
    *   **점수 기반 가속 엔진:** 100점 단위로 게이트 하강 속도, 생성 간격, 배경 차선 속도가 실시간으로 빨라지는 난이도 곡선 구현.
    *   **속도 제어 가이드 제공:** `GameFragment` 내 난이도 상수(`gateDuration`, `interval` 등)를 명확히 분리하여 향후 조정 용이성 확보.
- **사운드 시스템 안정화 및 볼륨 최적화:**
    *   **시스템 클릭음 제어:** 불필요한 시스템 기본 클릭음을 제거하고, 앱 내 모든 UI 인터랙션에 아주 미세한 커스텀 클릭음(0.1배) 연결.
    *   **피드백 사운드 강화:** 정답(`sfx_correct`) 및 오답(`sfx_wrong`) 효과음 볼륨을 2.0배로 상향하여 게임 몰입감 증대.
    *   **자원 관리 정교화:** `MediaPlayer` 해제 시 리스너 제거 및 리셋 프로세스를 강화하여 로그캣의 자원 관련 경고(`mediaplayer went away`) 해결.
    *   **안정적 재생 보장:** 비동기 준비(`prepareAsync`) 및 폴백(Fallback) 로직을 통해 파일 부재나 시스템 지연 시에도 사운드가 끊기지 않도록 보완.

### 🚀 향후 작업 계획 (Next Steps)
1. **마무리 점검:** 최종 빌드 테스트 및 배포 준비를 위한 전체 시스템 정밀 점검.

---

## 📅 2026-06-03-11 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 💎 사운드 정책 고도화 및 UI/UX 시각적 완성)
- **BGM 전환 로직 및 재생 정책 정교화:**
    *   **즉각적인 분위기 전환:** 게임 시작 버튼 클릭 후 카운트다운(3, 2, 1)이 시작되는 시점부터 전투 BGM으로 즉시 전환되도록 로직 개선.
    *   **시작 화면 정적 유지:** `MainActivity`(타이틀 화면)에서 불필요한 사운드 재생을 제거하여 게임 진입 시의 임팩트 강화.
    *   **트랙 확장 및 연동:** 메인 배경음악을 3가지 트랙으로 확장하고 설정창에서 자유롭게 선택 가능하도록 구현.
- **UI 정보 제공 및 가시성 극대화:**
    *   **실시간 날짜 시스템 도입:** 홈 화면 상단에 닉네임 대신 현재 날짜와 요일(년/월/일/요일)을 실시간으로 표시하여 정보성 강화.
    *   **학습 배경 선명도 개선:** `LearnFragment`의 배경 이미지 투명도를 1.0으로 상향하여 스테이지 맵의 가시성과 테마 몰입감 증대.
- **사운드 리소스 구조 재정립:**
    *   **효과음 분리:** 사용자 요청에 따라 정답(`sfx_correct`)과 오답(`sfx_wrong`) 효과음을 다시 별도의 리소스로 분리하여 명확한 청각적 피드백 제공.

### 🚀 향후 작업 계획 (Next Steps)
1. **최종 밸런싱 및 마무리 준비:** 게임 난이도 곡선(속도, 문제 빈도) 최종 조절 및 프로젝트 마무리 점검.

---

## 📅 2026-06-03-10 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🎵 사운드 시스템 동작 규칙 정밀화 및 사용자 정의 기능 완성)
- **배경음악(BGM) 재생 정책 최적화:**
    *   **학습 몰입도 강화:** `StudyActivity`(학습 창) 진입 시 BGM을 자동 중단하여 학습 집중도 향상.
    *   **자동 복구 시스템:** 학습 종료 후 메인 화면 복귀 시 설정된 BGM이 끊김 없이 다시 재생되도록 `onResume` 핸들링 구현.
    *   **무한 반복 및 유지:** 모든 BGM에 `isLooping` 설정을 적용하고, 동일 트랙 재생 요청 시 소리가 끊기지 않도록 중복 재생 방지 로직 적용.
- **사운드 구성 및 설정 고도화:**
    *   **효과음(SFX) 슬림화:** 사용자 요청에 따라 불필요한 클릭음을 제거하고, 정답/오답 효과음을 단일 리소스(`sfx_game`)로 통합하여 리소스 효율성 제고.
    *   **다중 트랙 선택 시스템:** 설정 창에서 메인 BGM과 게임 BGM을 각각 2가지 트랙 중 선택할 수 있는 UI 및 저장 시스템 완비.

### 🚀 향후 작업 계획 (Next Steps)
1. **고민중:** 현재까지의 전체적인 시스템 완성도를 점검하며 추가 기능 또는 밸런싱 요소 검토.

---

## 📅 2026-06-03-9 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🎵 사운드 엔진 고도화 및 설정 시스템 실질 연동)
- **사운드 시스템 인프라 구축:**
    *   **동적 로딩 도입:** `SoundManager`에 파일명 기반 리소스 로딩 방식(`getIdentifier`)을 적용하여, 파일이 없어도 앱이 안정적으로 구동되도록 설계.
    *   **상황별 BGM 자동 전환:** 메인/학습 화면(`bgm_main`)과 실제 게임 중(`bgm_game`) 배경음악이 상황에 맞게 자동으로 교체되는 로직 구현.
    *   **효과음(SFX) 전면 배치:** 모든 버튼 클릭, 정/오답 판정 시점에 적절한 효과음(`sfx_click`, `sfx_correct`, `sfx_wrong`) 매핑 완료.
- **사용자 설정 기능 완벽 연동:**
    *   **실시간 볼륨 계산 엔진:** 마스터 볼륨과 개별 볼륨(BGM/SFX)의 가중치를 계산하여 사운드 엔진에 즉각 반영하는 로직 완성.
    *   **진동 피드백 제어:** 유저가 설정한 진동 활성화 여부에 따라 게임 중 오답 피드백(Vibration)이 작동하도록 연동.
    *   **데이터 영속성 확보:** 볼륨, 진동, FPS 설정값이 앱 종료 후에도 유지되도록 `SharedPreferences` 기반 저장 시스템 정밀화.

### 🚀 향후 작업 계획 (Next Steps)
1. **음악 파일 추가:** 정리된 리스트(`bgm_main`, `bgm_game`, `sfx_...`)에 해당하는 실제 오디오 파일(.mp3, .wav) 준비 및 `res/raw` 배치.
2. **최종 밸런싱:** 게임 난이도(게이트 속도, 문제 출현 확률) 및 보상 체계 최종 조절.

---

## 📅 2026-06-03-8 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🧹 학습 탭 레이아웃 간소화 및 시각적 정돈)
- **학습 탭(Learn) 구조 대폭 간소화:**
    *   불안정하거나 시각적으로 불분명했던 스테이지 간 연결선(Line) 및 관련 수학적 회전 로직을 전면 삭제하여 시스템 안정성 확보.
    *   부유 애니메이션, 화살표 인디케이터 등 불필요한 시각적 장식을 제거하고 스테이지 노드 배치(지그재그) 본연의 기능에 집중하도록 디자인 정돈.
    *   아이템 높이를 140dp로 고정하여 스크롤 시의 균형감과 리스트 가독성 개선.
- **프로젝트 무결성 유지:**
    *   미사용 리소스 및 컴파일 에러를 유발할 수 있는 테마 참조 로직을 정리하여 코드 베이스 최적화.

### 🚀 향후 작업 계획 (Next Steps)
1. **사운드 리소스 통합:** `SoundManager`에 실제 오디오 파일을 매핑하고 상황별 효과음/배경음 재생 완성.
2. **설정 관련 작동:** 설정창의 볼륨, 진동, FPS 옵션이 실제 게임 엔진과 UI에 완벽히 연동되도록 구현.
3. **최종 밸런싱:** 게임 난이도(게이트 속도, 문제 출현 로직) 및 보상 체계 최종 조절.

---

## 📅 2026-06-03-7 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 📏 UI 밀도 최적화 및 스테이지 연결선 정밀 보정)
- **단어장(Word List) 가독성 향상:**
    *   리스트 아이템의 상하 길이를 1.15배 확장(Padding 23dp)하여 시각적 답답함 해소 및 터치 편의성 증대.
    *   단어 및 뜻의 텍스트 크기를 1.1배 확대하여 정보 전달력 강화.
- **전투(Game) UI 레이아웃 정교화:**
    *   **문제 카드 노출 제어:** 미션 선택 화면에서 영어 단어 블록을 숨김 처리하고, 게임이 실제 시작되는 시점에만 나타나도록 로직 수정.
    *   **점수 HUD 개선:** 점수(Score) 전용 카드 컨테이너를 추가하여 배경과 시각적으로 분리하고 가시성을 대폭 강화.
- **스테이지 맵(Learn) 연결 시스템 안정화:**
    *   **연결선 길이 및 가시성 확보:** 선의 길이를 기존 대비 1.5배 연장하여 지그재그 배치된 노드 사이가 끊김 없이 명확히 이어지도록 수정.
    *   **디자인 보정:** 선 두께를 8dp로 증폭하고 투명도를 0.4로 상향하여 경로 가이드의 시인성 확보.

### 🚀 향후 작업 계획 (Next Steps)
1. **학습 탭 전면 수정:** 스테이지 맵의 구조와 인터랙션을 사용자 경험에 맞춰 근본적으로 재검토 및 수정.
2. **사운드 리소스 통합:** `SoundManager`에 실제 오디오 파일을 매핑하고 상황별 효과음/배경음 재생 완성.
3. **설정 관련 작동:** 설정창의 볼륨, 진동, FPS 옵션이 실제 게임 엔진과 UI에 완벽히 연동되도록 구현.
4. **최종 밸런싱:** 게임 난이도(게이트 속도, 문제 출현 로직) 및 보상 체계 최종 조절.

---

## 📅 2026-06-03-6 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 📍 연결선 정밀화 및 학습 UI 미세 조정)
- **스테이지 맵(Learn) 연결선 엔진 재설계:**
    *   **중점 이동(Midpoint Translation) 로직 도입:** 노드와 노드 사이의 정중앙 지점을 계산하여 선을 배치하고 회전시키는 방식으로 변경하여, 어떤 해상도에서도 노드 중심을 정확히 잇도록 개선.
    *   **수학적 각도 보정:** `atan2` 기반의 정밀한 각도 계산을 통해 지그재그 배치된 스테이지 간의 연결성 확보.
    *   **시각적 최적화:** 연결선 두께를 6dp로 증폭하고 투명도를 0.3으로 조정하여 세련된 디자인 구현.
- **학습 화면(Study Activity) 레이아웃 2차 미세 조정:**
    *   진도바, 단어 카드, 뜻 카드, 별표 버튼의 상단 여백(Margin Top)을 대폭 축소하여 전체 콘텐츠를 화면 상단으로 이동시켜 시각적 답답함 해소.
    *   요소 간의 간격을 좁혀 학습 영역의 밀도와 가독성을 높임.
- **스타(Star) 디자인 디테일 개선:**
    *   `ic_star_hollow.xml`(미암기 상태)의 테두리 두께를 2.0에서 **1.2**로 슬림화하여 현대적인 느낌 강화.
- **데이터 무결성 보장:**
    *   `WordDatabase`의 초기화 로직을 보완하여 앱 실행 시마다 단어가 중복 주입되는 현상 해결 (`existingCount > 0` 체크).

### 🚀 향후 작업 계획 (Next Steps)
1. **사운드 리소스 통합:** `SoundManager`에 실제 오디오 파일 매핑.
2. **최종 밸런싱:** 게임 난이도 및 보상 체계 최종 조절.

---

## 📅 2026-06-03-5 업데이트 로그

### 🛠 현재까지 완료된 작업 (Status: 🎨 디자인 전면 개편 및 UI/UX 디테일 강화)
- **전체 디자인 테마 변경:**
    *   기존의 브라운 테마에서 세련된 **네이비 & 쿨톤(Navy Cool Tone)**으로 전면 개편하여 전문적이고 현대적인 느낌 강화.
    *   상단 바(AppBar)와 콘텐츠 영역의 대비를 명확히 하여 시인성 확보.
- **학습 화면(Study Activity) 레이아웃 재설계:**
    *   **헤더 확장:** 뒤로가기 버튼과 스테이지 제목의 크기를 1.5배~2배 확대하여 가독성 개선.
    *   **진행바 고도화:** 진행 상황 텍스트(예: 1/20)를 진행바 상단으로 이동 및 크기 확대, 진행바 두께를 키워 시각적 인지 향상.
    *   **간격 최적화:** 모든 요소를 아래로 약 10~20% 이동시켜 상단 쏠림 현상 해결 및 수직 균형 확보.
    *   **네비게이션 버튼:** PREV/NEXT 버튼의 가로 너비를 35%로 확장하고 좌우/중앙 공백을 배치하여 조작성 개선.
- **스타(Star) 시스템 및 디자인 통일:**
    *   **아이콘 변경:** 초록색 스타 대신 **빈 검은색 테두리 스타(미암기)**와 **노란색 채워진 스타(암기)** 리소스로 교체하여 직관적인 피드백 제공.
    *   **단어장 연동:** 단어장(`WordListActivity`, `WordbookFragment`) 리스트에서 스타를 직접 클릭하여 암기 여부를 즉시 토글할 수 있는 기능 추가.
- **스테이지 맵(Learn) 연결선 정밀 조정:**
    *   스테이지 아이콘 사이를 잇는 라인이 실제 다음 스테이지를 향하도록 각도 및 피벗(Pivot) 로직 수정.
- **로컬라이징 완벽 적용:**
    *   기본 `values/strings.xml`을 한국어로 교체하여 모든 환경에서 한국어가 기본 노출되도록 강제 설정 (PREV/NEXT -> 이전/다음 등).
- **로그 관리 체계 구축:**
    *   `LOG.md`를 생성하여 오래된 로그를 아카이빙하고, `CONTEXT.md`에는 최신 5개의 로그만 유지하도록 관리 기준 수립.

### 🚀 향후 작업 계획 (Next Steps)
1. **사운드 리소스 통합:** `SoundManager`에 실제 오디오 파일 매핑.
2. **최종 밸런싱:** 게임 난이도 및 보상 체계 최종 조절.

---

## 📅 2026-06-03-4 업데이트 로그
... (LOG.md 참조)
